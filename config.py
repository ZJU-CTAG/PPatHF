# we use LLM_PATH + LLM_NAME_PATH_MAPPING[llm_name] to determine the weight path of a given llm_name
LLM_PATH = "xxx"
LLM_NAME_PATH_MAPPING = {"starcoder": "xxx"}

MODEL_PATH = "xxx"
DATA_PATH = "xxx"
OUTPUT_PATH = "xxx"

EXTRACTED_METRIC_PATH = f"xxx"